<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="text/html" href="../styles.css" />
    <title>Login</title>
    <!-- <link rel="stylesheet" href="../index.css" /> НЕПОНИМАЮ ПОЧЕМУ НЕПОДКЛЮЧАЕТСЯ -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous"
    />
    <style>
      * {
        box-sizing: border-box;
      }

      /* Поля ввода полной ширины */
      input[type="email"],
      input[type="password"] {
        width: 300px;
        padding: 15px;
        margin: 5px 0 22px 0;
        display: flex;
        flex-direction: column;
        border: none;
        background: #f1f1f1;
      }

      input[type="email"]:focus,
      input[type="password"]:focus {
        background-color: #ddd;
        outline: none;
      }

      /* Установите стиль для всех кнопок */
      button {
        background-color: #4caf50;
        color: white;
        padding-left: 50px;
        margin-left: 60px;
        padding: 10px;
        border: none;
        cursor: pointer;
        width: 100%;
        opacity: 0.9;
      }

      button:hover {
        opacity: 1;
      }

      .signupbtn {
        float: left;
        width: 50%;
      }

      /* Добавить отступы к элементам контейнера */
      .container {
        padding: 60px;
        margin: 40px;
        display: flex;
        justify-content: center;
      }

      /* Очистить поплавки */
      .clearfix::after {
        content: "";
        clear: both;
        display: table;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <form
        action="/operator"
        method="post"
        style="border: 1px solid #ccc; margin: 10px; padding: 10px"
      >
        <h1 style="text-align: center">Вход</h1>
        <label for="email"><b>Электронная почта</b></label>
        <input
          type="email"
          placeholder="Введите вашу почту"
          name="email"
          required
        />

        <label for="psw"><b>Пароль</b></label>
        <input
          type="password"
          placeholder="Введите пароль"
          name="password"
          required
        />
        <div class="clearfix">
          <button type="submit" class="signupbtn">Войти</button>
        </div>
        <% if (error) {%>
        <div class="alert alert-danger" style="margin: 10px"><%= error %></div>
        <% } %>
      </form>
    </div>
  </body>
</html>
